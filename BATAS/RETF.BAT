IF %MODE%==CLEAN GOTO :EOF

IF NOT x%1==x GOTO :OFFT

CALL RPN! _%IP%_1+
SET IP=%RESULT%
IF %MODE%==COUNT GOTO :EOF
CALL EMITBYTE 203

GOTO :EOF

:OFFT
CALL RPN! _%IP%_3+
SET IP=%RESULT%
IF %MODE%==COUNT GOTO :EOF
CALL EMITBYTE 202
CALL EMITWORD %1

:EOF
